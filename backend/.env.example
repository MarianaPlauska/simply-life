# Copie este arquivo para .env e preencha os valores reais
# cp .env.example .env

# PostgreSQL (Supabase direct connection)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.zuxkqmooxvnulgllduhr.supabase.co:5432/postgres"

# JWT (gerar: python -c "import secrets; print(secrets.token_urlsafe(48))")
JWT_SECRET=""

# Fernet AES-256 (gerar: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FERNET_KEY=""

# Google OAuth2 (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI="http://localhost:5173/google-callback"
FRONTEND_URL="http://localhost:5173"
