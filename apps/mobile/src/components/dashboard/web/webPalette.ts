/**
 * `colors.hairline` (#2A2A2C) é quase idêntico a `colors.elevated` (#1C1C1E)
 * — a diferença é real no código mas invisível a olho nu sobre fundo quase
 * preto, então as bordas "sumiam". Sistemas escuros de referência (Linear,
 * Vercel, GitHub) usam uma borda clara translúcida (rim light) em vez de
 * cinza escuro — é isso que dá definição real a um cartão sobre preto.
 */
export const WEB_CARD_BORDER = 'rgba(245, 241, 236, 0.12)'
export const WEB_ROW_DIVIDER = 'rgba(245, 241, 236, 0.07)'
