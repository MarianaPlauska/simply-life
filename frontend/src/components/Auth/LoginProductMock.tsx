/** Palco 3D: notebook, telefone e cards flutuantes (render). */
export function LoginProductMock()
{
  return (
    <div className="relative w-full h-full min-h-[420px] pointer-events-none select-none" aria-hidden>
      <img
        src="/login-hero-stage.png"
        alt=""
        className="absolute inset-0 h-full w-full object-contain object-bottom"
      />
    </div>
  )
}
