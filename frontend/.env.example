VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# NUNCA defina VITE_GROQ_API_KEY nem VITE_GEMINI_API_KEY em produção.
# Chaves de IA ficam só no servidor (GROQ_API_KEY na raiz + vercel dev / Vercel).
# O fallback VITE_* só funciona em `npm run dev` local (import.meta.env.DEV).
